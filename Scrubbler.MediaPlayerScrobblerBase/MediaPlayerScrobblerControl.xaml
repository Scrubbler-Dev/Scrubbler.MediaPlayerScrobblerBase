<UserControl
    x:Class="MediaPlayerScrobblerBase.MediaPlayerScrobblerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaPlayerScrobblerBase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Padding="12" RowSpacing="16" ColumnSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- STATUS BAR -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">

            <!-- Status Label -->
            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <TextBlock Text="Status:" FontWeight="Bold"/>

                <TextBlock x:Name="StatusText" Text="{Binding ConnectionStatusText}">
                </TextBlock>
            </StackPanel>

            <!-- Connect / Reconnect -->
            <Button x:Name="ConnectButton" Command="{Binding ToggleConnectionCommand}" Content="{Binding ConnectionButtonText}">
            </Button>

            <!-- Auto-Connect -->
            <CheckBox Content="Auto-Connect" IsChecked="{Binding AutoConnect, Mode=TwoWay}"
                  VerticalAlignment="Center"/>

            <!-- Rich Presence -->
            <CheckBox Content="Discord Rich Presence" IsChecked="{Binding EnableDiscordRichPresence, Mode=TwoWay}"
                  VerticalAlignment="Center"/>
        </StackPanel>

        <Rectangle Grid.Row="1" Height="1" Fill="Gray" Opacity="0.3"/>

        <!-- MAIN CONTENT AREA -->
        <Grid Grid.Row="2" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Album Art -->
            <Border BorderBrush="Black" BorderThickness="1" Width="150" Height="150" VerticalAlignment="Top">
                <Image Stretch="UniformToFill">
                    <Image.Source>
                        <BitmapImage UriSource="{Binding CurrentAlbumArtwork, TargetNullValue=ms-appx:///Assets/noartistimage.png}" />
                    </Image.Source>
                </Image>
            </Border>

            <!-- Metadata -->
            <StackPanel Grid.Column="1" Spacing="8">

                <!-- TRACK -->
                <StackPanel>
                    <TextBlock Text="Track:" FontWeight="Bold"/>

                    <HyperlinkButton Content="{Binding CurrentTrackName}"
                                 Foreground="Blue"
                                 Command="{Binding TrackClickedCommand}"/>

                    <StackPanel x:Name="TrackPlayCount" Visibility="{Binding TrackPlayCountVisibility}" Orientation="Horizontal" Spacing="4">

                        <TextBlock Text="{Binding CurrentTrackPlayCount}"/>
                        <TextBlock Text="Plays"/>
                    </StackPanel>
                </StackPanel>

                <!-- ARTIST -->
                <StackPanel>
                    <TextBlock Text="Artist:" FontWeight="Bold"/>
                    <HyperlinkButton Content="{Binding CurrentArtistName}"
                                 Foreground="Blue"
                                 Command="{Binding ArtistClickedCommand}"/>

                    <StackPanel x:Name="ArtistPlayCount" Visibility="{Binding ArtistPlayCountVisibility}" Orientation="Horizontal" Spacing="4">

                        <TextBlock Text="{Binding CurrentArtistPlayCount}"/>
                        <TextBlock Text="Plays"/>
                    </StackPanel>
                </StackPanel>

                <!-- ALBUM -->
                <StackPanel>
                    <TextBlock Text="Album:" FontWeight="Bold"/>
                    <HyperlinkButton Content="{Binding CurrentAlbumName}"
                                 Foreground="Blue"
                                 Command="{Binding AlbumClickedCommand}"/>

                    <StackPanel x:Name="AlbumPlayCount" Visibility="{Binding AlbumPlayCountVisibility}" Orientation="Horizontal" Spacing="4">

                        <TextBlock Text="{Binding CurrentAlbumPlayCount}"/>
                        <TextBlock Text="Plays"/>
                    </StackPanel>
                </StackPanel>

                <!-- TAGS -->
                <StackPanel x:Name="TagPanel" Visibility="{Binding TagsVisibility}">

                    <TextBlock Text="Tags:" FontWeight="Bold"/>
                    <ItemsRepeater ItemsSource="{Binding CurrentTrackTags}">
                        <ItemsRepeater.Layout>
                            <FlowLayout Orientation="Horizontal" />
                        </ItemsRepeater.Layout>

                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate>
                                <HyperlinkButton Content="{Binding Name}"
                                             Foreground="Blue"
                                             Command="{Binding ClickedCommand}"/>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </StackPanel>

                <!-- LOVE BUTTON -->
                <Button x:Name="LoveButton" Command="{Binding ToggleLovedStateCommand}" Content="{Binding LoveButtonText}" IsEnabled="{Binding IsConnected}" Visibility="{Binding LoveButtonVisibility}">
                </Button>

                <!-- SCROBBLE METER -->
                <StackPanel>
                    <TextBlock Text="Scrobble-Meter:" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding CountedSeconds}" Maximum="{Binding CurrentTrackLengthToScrobble}"/>

                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock x:Name="ScrobbleCount" Text="{Binding ScrobbleProgressSeconds}">
                        </TextBlock>
                        <TextBlock Text="/"/>
                        <TextBlock Text="{Binding CurrentTrackLengthToScrobble}"/>
                        <TextBlock Text="seconds"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid x:Name="NotConnectedGrid" Visibility="{Binding NotConnectedVisibility}" Grid.Row="2" Opacity="0.8" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

            <Border>
                <Border.Background>
                    <AcrylicBrush AlwaysUseFallback="False"
                      TintColor="#80000000"
                      TintOpacity="0.1"/>
                </Border.Background>
            </Border>

            <TextBlock Text="Not connected" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

    </Grid>
</UserControl>
